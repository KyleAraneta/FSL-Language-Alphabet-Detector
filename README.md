Note:
Large training CSV files are not included in this GitHub repo because they exceed GitHub's file size limit.

The app can still run using the included trained .joblib model files.

If you want to retrain the model, download the CSV dataset from the shared Google Drive link and place it in:

data/phrases/fsl_phrase_data.csv
